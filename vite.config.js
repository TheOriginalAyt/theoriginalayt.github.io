import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Set base to relative so assets resolve properly on GitHub Pages project subpaths
  base: './',
});
